#### modemloader
#
type modemloader, coredomain, domain;
type modemloader_exec, system_file_type, exec_type, file_type;

init_daemon_domain(modemloader)

allow modemloader proc:file r_file_perms;

set_prop(modemloader, modemloader_prop)
